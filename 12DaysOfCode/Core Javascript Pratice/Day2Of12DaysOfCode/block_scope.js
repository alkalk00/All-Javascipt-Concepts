
        if(true){
            //var is a datatype that is linked with windows.ojects and it's scope can be accesed outside block
            //let is introduced in ES5 that is specific to block of brackets only
            //const is also same as the let
            var x = 10;
            console.log(x);
            let k = 20;
            console.log(k);
        }
        console.log(x);
        //let does't allow it's variable to call outside it's block
        //console.log(k);
   